{
  "name": "spring-integration-progress-bar",
  "version": "1.0.0",
  "jest": {
    "testPathIgnorePatterns": [
      "target"
    ]
  },
  "scripts": {
    "test": "jest",
    "watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.3.1"
  }
}
