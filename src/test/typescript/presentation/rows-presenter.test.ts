import { describe, test } from 'vitest';

describe('rows-presenter', () => {
    test('renders ... rows ...', () => {
        // give the presenter a flowProgressContainer

        // receive back a list of flowProgressRows
    })
});
